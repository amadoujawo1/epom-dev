# ePOM Client

React + Vite starter for the private office management system.

Install:

```bash
cd client
npm install
npm run dev
```

The dev server expects the Flask backend to be running on port 5000. You can configure proxying in `vite.config.js` if desired.
